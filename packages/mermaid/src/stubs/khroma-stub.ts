/**
 * Khroma color library stub for validation-only build
 */

export const adjust = (color: string) => color;
export const darken = (color: string) => color;
export const invert = (color: string) => color;
export const isDark = (color: string) => false;
export const lighten = (color: string) => color;
export const rgba = (color: string) => color;

export default {
  adjust,
  darken,
  invert,
  isDark,
  lighten,
  rgba,
};